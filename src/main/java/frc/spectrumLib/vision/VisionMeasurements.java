package frc.spectrumLib.vision;

public class VisionMeasurements {
    // Targets in view?
    // Target Size? reject if target is too small
    // ambiguity?
    // Pose estimate (is it off the field, in a place where we can't be?, is the robot tilted, or
    // floating in air,)
    // Robot rotating speed

    // If using megaTag2 don't integrate angle at all, since it's using our gyro angle

}
